{
  "name": "ai-chat-sprawler",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Chrome extension to branch new chat sessions from selected text in AI chatbot responses.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.34",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
